apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: ai-portal-librechat-policy
    files:
      - librechat.yaml
      - admin-override.json
generatorOptions:
  disableNameSuffixHash: true
